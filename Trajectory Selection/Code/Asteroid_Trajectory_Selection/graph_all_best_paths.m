for n = 1:length(i_mins)
    FLIGHTPATH_ANIMATION(asteroid_optimized_data(i_mins(n), j_mins(n), k_mins(n)), ...
        asteroid_list, i_mins(n), j_mins(n), k_mins(n), 10, ...
        sprintf("%s_%s_%s_2D.mp4", asteroid_list(i_mins(n)).NAME, ...
        asteroid_list(j_mins(n)).NAME, asteroid_list(k_mins(n)).NAME));

    close all force;
end